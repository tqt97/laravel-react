# Database Rules

Production database: PostgreSQL.

Money is stored as integer minor units.

Booking overlap protection must be enforced in the database.

All multi-record booking writes happen inside transactions.

Never add database indexes without checking the target query.

Money is stored as integer minor units.

Booking overlap protection must be enforced in the database.

All multi-record booking writes happen inside transactions.

Never add database indexes without checking the target query.
